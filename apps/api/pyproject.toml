[project]
name = "barthe-api"
version = "1.0.0"
description = "BARTHE — Plateforme SaaS d'analyse de Business Plans. API FastAPI."
requires-python = ">=3.13"

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
]
